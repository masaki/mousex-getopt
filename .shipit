steps = FindVersion, ChangeAllVersions, CheckVersionsMatch, CheckChangeLog, Manifest, DistTest, Commit, Tag, MakeDist, UploadCPAN, Twitter, DistClean

git.tagpattern = %v
git.push_to = origin

twitter.config = ~/.pit/twitter.yaml
